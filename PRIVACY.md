# Privacy Policy — Chalkieboard iCloud MCP / 개인정보 처리방침 — 초키보드 iCloud MCP

Last updated: 2026-09-20 · Contact: chalkieboard@icloud.com

## English

**What this software does.** This is a local MCP (Model Context Protocol) server. It gives an AI client on your own computer (for example Claude Desktop) tools to read and write files inside one folder: the iCloud Drive folder that the Chalkieboard iPad app maintains for your lesson materials (`iCloud~only1mui~ChalkieBoard/Documents`, or a folder you choose in the extension settings).

**What data it accesses.** Only the files in that folder: subject folders, lesson materials (PDF, PPTX, HTML), and the app-generated index files (`structure.json`, `README.md`, `lessons.chalkie.json`). It never opens files outside that folder, and path arguments are checked so they cannot escape it.

**What it sends anywhere.** Nothing. The server makes **no network connections** of any kind. It has no telemetry, no analytics, no accounts, no crash reporting, and no third-party dependencies. Any data the AI client sees comes from tool results you invoke, and is handled under that client's own privacy policy.

**What it stores.** Nothing beyond the files it writes into the folder at your request (materials and lesson-plan entries). It keeps no cache, log, or settings of its own.

**Deletion.** The server has no delete tool. To remove data, delete files in the folder or in the Chalkieboard app.

**Children's data / student data.** The Chalkieboard folder contains a teacher's lesson materials. The app itself does not store student names or personal information in this folder, and this server adds none.

**Changes.** If this policy changes, the new version is published at the same URL with an updated date.

## 한국어

**하는 일.** 이 소프트웨어는 컴퓨터 안에서만 도는 로컬 MCP 서버입니다. 사용자의 AI 클라이언트(예: Claude Desktop)에 도구를 주어, 초키보드 아이패드 앱이 관리하는 iCloud Drive 수업 폴더(`iCloud~only1mui~ChalkieBoard/Documents`, 또는 확장 설정에서 고른 폴더) 하나 안의 파일을 읽고 쓰게 합니다.

**접근하는 데이터.** 그 폴더 안의 파일뿐입니다 — 과목 폴더, 수업 자료(PDF·PPTX·HTML), 앱이 만든 목록 파일(`structure.json`·`README.md`·`lessons.chalkie.json`). 폴더 밖 파일은 열지 않으며, 경로 인자는 폴더 밖으로 나가지 못하게 검사합니다.

**어디로 보내는가.** 아무 데도 보내지 않습니다. 이 서버는 **어떤 네트워크 연결도 하지 않습니다.** 원격 측정·분석·계정·오류 보고·외부 의존성이 없습니다. AI 클라이언트가 보는 데이터는 사용자가 부른 도구의 결과이며, 그 클라이언트의 개인정보 처리방침을 따릅니다.

**저장하는 것.** 사용자의 요청으로 폴더에 쓰는 파일(자료·차시 계획 항목) 외에는 없습니다. 자체 캐시·로그·설정을 두지 않습니다.

**삭제.** 지우는 도구가 없습니다. 데이터를 지우려면 폴더에서 파일을 지우거나 초키보드 앱에서 지웁니다.

**학생 정보.** 초키보드 폴더에는 교사의 수업 자료가 있습니다. 앱은 이 폴더에 학생 이름이나 개인정보를 저장하지 않으며, 이 서버도 추가하지 않습니다.

**변경.** 방침이 바뀌면 같은 주소에 날짜를 고쳐 게시합니다.
